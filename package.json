{
  "name": "kitsos-api-monorepo",
  "private": true,
  "scripts": {
    "check": "tsc -p packages/auth/tsconfig.json --noEmit && tsc -p apps/keys-api/tsconfig.json --noEmit && tsc -p apps/verify/tsconfig.json --noEmit",
    "test": "npm run check && sh tests/security-invariants.sh"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
